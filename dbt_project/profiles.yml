hotel_pipeline:
  target: dev
  outputs:
    dev:
      type: postgres
      host: localhost
      port: 5433
      user: dbt_user
      password: dbt_password
      dbname: hotel_dw
      schema: public
      threads: 4

    docker:
      type: postgres
      host: hotel_postgres
      port: 5432
      user: dbt_user
      password: dbt_password
      dbname: hotel_dw
      schema: public
      threads: 4