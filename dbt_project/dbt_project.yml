name: 'hotel_pipeline'
version: '1.0.0'
config-version: 2

profile: 'hotel_pipeline'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  hotel_pipeline:
    staging:
      +materialized: view
    marts:
      +materialized: table